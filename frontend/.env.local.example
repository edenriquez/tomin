# Base URL of the Tomin backend API.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase (optional in dev; auth is disabled backend-side by default).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
