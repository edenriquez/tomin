# Where the "Comenzar" buttons send people: the deployed Tomin app.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Public URL of this landing (metadataBase / OG). On Vercel it falls back to
# VERCEL_PROJECT_PRODUCTION_URL, so this is only needed for custom domains.
# NEXT_PUBLIC_SITE_URL=https://tomin.mx
